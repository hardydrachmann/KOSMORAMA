angular.module('kosmoramaApp').service('downloadService', function($cordovaFileTransfer) {

	document.addEventListener("deviceready", onDeviceReady, false);

	function onDeviceReady() {
		// alert(FileTransfer);
	}


});
